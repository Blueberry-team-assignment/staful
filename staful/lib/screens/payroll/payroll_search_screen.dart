import 'package:flutter/material.dart';

class PayrollSearchScreen extends StatelessWidget {
  const PayrollSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
